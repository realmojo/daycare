/**
 * 애드센스 광고 단위.
 *
 * 퍼블리셔 ID 는 keywordegg 계열과 같은 계정(ca-pub-9130836798889522)이라
 * 새로 승인받을 필요가 없다. 다만 **슬롯은 이 사이트용으로 따로 만든 것**을
 * 쓴다. 다른 사이트 슬롯을 그대로 쓰면 애드센스 리포트에서 수익이 섞여
 * 어느 사이트가 버는지 구분되지 않는다.
 *
 * 슬롯 ID 가 빈 문자열이면 <Adsense> 가 아무것도 그리지 않는다.
 */
export const AD_CLIENT = "ca-pub-9130836798889522";

export const AD_SLOTS = {
  /** 어린이집(글지역) — 홈·목록 화면 */
  home: "4198615904",
  /** 어린이집(상단) — 본문 맨 위 */
  top: "3787552360",
  /** 어린이집(CTA) — 소개글·행동 버튼 다음 */
  middle: "2885534232",
  /** 어린이집(h2) — 본문 중간 h2 아래, 목록 하단 */
  bottom: "4144298095",
} as const;
